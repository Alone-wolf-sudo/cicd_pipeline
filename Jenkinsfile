pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git 'https://github.com/Alone-wolf-sudo/cicd_pipeline.git'
            }
        }
    }
}